<div class="fixed-right shadow bg-white p-4 pe-3 col-3 m-3 mt-5">
  <h5 class="text-center">
    Photo <span class="text-muted">generation <%= @generation.title %></span>
  </h5>
  <br>

  <%= image_tag(@generation.avatar.url, width: '420') if @generation.avatar.present? %>
</div>
